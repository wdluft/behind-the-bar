# drink name
# [ingredients]
# recipe
# created/added

type Drink {
  _id: String!
  name: String!
}

# type Query {
#   drink(id: ID): Drink
# }

type Mutation {
  createDrink(name: String!): Drink
  removeDrink(id: ID!): [Drink]
}
